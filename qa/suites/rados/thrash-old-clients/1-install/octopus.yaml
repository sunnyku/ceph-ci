overrides:
  ceph:
    log-whitelist:
      - \(MON_DOWN\)
tasks:
- install:
    branch: octopus
    exclude_packages:
      - ceph-mgr-dashboard
      - ceph-mgr-diskprediction-cloud
      - ceph-mgr-diskprediction-local

